# Optional: set full API URL if backend is hosted elsewhere.
# Leave empty to use the Vite proxy with /api in local development.
VITE_API_BASE_URL=

# Backend origin used by Vite proxy when VITE_API_BASE_URL is empty.
VITE_BACKEND_ORIGIN=http://127.0.0.1:8080
